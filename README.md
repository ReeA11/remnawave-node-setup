# 🚀 RemnaNode Setup

<div align="center">

**Автоматическая установка и управление Remnawave Node на вашем сервере**

[![Shell Script](https://img.shields.io/badge/Shell-Bash-green.svg)](https://www.gnu.org/software/bash/)
[![Docker](https://img.shields.io/badge/Docker-Required-blue.svg)](https://www.docker.com/)

[Быстрый старт](#-быстрый-старт) • [Установка](#-установка) • [Удаление](#-удаление) • [Безопасность](#-безопасность) • [FAQ](#-faq)

</div>

---

## 📋 Содержание

- [Описание](#-описание)
- [Быстрый старт](#-быстрый-старт)
- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Удаление](#-удаление)
- [Безопасность](#-безопасность)
- [Архитектура](#-архитектура)
- [Устранение неполадок](#-устранение-неполадок)
- [FAQ](#-faq)

---

## 🎯 Описание

RemnaNode Setup — это набор bash-скриптов для автоматизации установки, настройки и управления Remnawave Node. Скрипты обеспечивают:

- ✅ Полную автоматизацию процесса установки
- ✅ Поддержку множественных нод на одном сервере
- ✅ Автоматическую настройку сети и маршрутизации
- ✅ Управление через интерактивное меню
- ✅ Безопасное удаление с очисткой всех зависимостей

---

## ⚡ Быстрый старт

### Интерактивное меню (рекомендуется)

```bash
bash <(curl -Ls https://raw.githubusercontent.com/ReeA11/remnawave-node-setup/refs/heads/master/menu.sh)
```

Меню позволяет:
- Установить новую ноду
- Удалить существующую ноду
- Настроить ufw и запретить ping сервера

---

## 🎨 Возможности

### 🔧 Автоматическая установка
- **Выбор версии**: latest или определенная версия
- **Множественные ноды**: Поддержка установки нескольких нод на одном сервере
- **Умная маршрутизация**: Автоматическая настройка policy routing для multi-IP конфигураций
- **Docker интеграция**: Автоматическая установка Docker при его отсутствии

### 🌐 Сетевая настройка
- **Multi-IP support**: Работа с несколькими внешними IP на сервере
- **NAT & SNAT**: Автоматическая настройка трансляции адресов
- **UFW интеграция**: Автоматическое добавление правил файрвола
- **Persistent rules**: Сохранение сетевых правил после перезагрузки

### 🛡️ Безопасность
- **Точечные правила UFW**: Открытие портов только для выбранного интерфейса
- **Изоляция портов**: Предотвращение конфликтов портов

### 🗑️ Чистое удаление
- **Полная очистка**: Удаление контейнера, сетей, правил маршрутизации
- **Безопасность**: Проверки для предотвращения удаления системных настроек
- **UFW cleanup**: Автоматическое удаление созданных правил файрвола

---

## 📦 Требования

### Операционная система
- Ubuntu 20.04 / 22.04 / 24.04
- Debian 11 / 12

### Минимальные ресурсы
- **CPU**: 1 core
- **RAM**: 512 MB
- **Disk**: 1 GB свободного места

### Необходимое ПО
- `bash` 4.0+
- `curl`
- Root-доступ (sudo)

**Примечание**: Docker будет установлен автоматически, если отсутствует

---

## 🚀 Установка

### Способ 1: Через интерактивное меню (рекомендуется)

```bash
bash <(curl -Ls https://raw.githubusercontent.com/ReeA11/remnawave-node-setup/refs/heads/master/menu.sh)
```

Выберите пункт `1) Установить RemnaNode` и следуйте инструкциям.

### Способ 2: Прямая установка

```bash
bash <(curl -Ls https://raw.githubusercontent.com/ReeA11/remnawave-node-setup/refs/heads/master/setup-remnanode.sh)
```

### Процесс установки

Скрипт запросит следующую информацию:

1. **Порт для приложения** (по умолчанию: 2222)
   ```
   📝 Введите порт для приложения (по умолчанию 2222): 
   ```

2. **Выбор сетевого интерфейса**
   ```
   🌐 Доступные IPv4 адреса:
   [1] eth0      → 10.0.0.5    → внешний: 203.0.113.10
   [2] eth1      → 10.0.1.5    → внешний: 203.0.113.20
   
   👉 Выберите IP/интерфейс для этой ноды [1-2]: 
   ```

3. **SECRET_KEY**
   ```
   🔑 SECRET_KEY для этой ноды
   📝 Вставьте значение SECRET_KEY: 
   ```
   > ⚠️ Скрипт автоматически проверит уникальность SECRET_KEY

4. **Версия RemnaNode**
   ```
   📦 Выберите версию RemnaNode:
   [1] Old 2.2.3
   [2] latest
   [3] Выбрать версию вручную
   
   👉 Ваш выбор [1-3]: 
   ```

### Что происходит во время установки?

1. ✅ Проверка и установка зависимостей
2. ✅ Создание директории `/opt/remnanode` (или `/opt/remnanode2`, `/opt/remnanode3` и т.д.)
3. ✅ Генерация `docker-compose.yml` и `.env`
4. ✅ Запуск Docker контейнера
5. ✅ Настройка policy routing и NAT
6. ✅ Создание systemd service для персистентности правил
7. ✅ Добавление UFW правил (опционально)
8. ✅ Отображение логов контейнера

### Пример успешной установки

```
✔ Локальный IP интерфейса: 10.0.0.5 (внешний IP: 203.0.113.10)
✔ Интерфейс: eth0
✔ Выбрана версия: 2.2.3

📁 Будет использована директория: /opt/remnanode
🐳 Имя контейнера: remnanode

[*] Проверяю доступность порта и IP...
[*] Создаю .env...
[*] Создаю docker-compose.yml...
[*] Запускаю контейнер remnanode...
✔ Контейнер remnanode запущен

🌍 Доступ извне: 203.0.113.10:2222

🔀 Настраиваю routing/NAT для выхода ноды через eth0...
✔ Policy routing настроен
✔ NAT правила добавлены

🎉 Установка завершена! 🎉
```

---

## 🗑️ Удаление

### Через интерактивное меню

```bash
bash <(curl -Ls https://raw.githubusercontent.com/ReeA11/remnawave-node-setup/refs/heads/master/menu.sh)
```

Выберите пункт `2) Удалить RemnaNode`

### Прямое удаление

```bash
bash <(curl -Ls https://raw.githubusercontent.com/ReeA11/remnawave-node-setup/refs/heads/master/remove-remnanode.sh)
```

### Процесс удаления

1. Скрипт найдет все установленные ноды:
   ```
   📦 Найденные ноды:
   
   [1] remnanode     → 🟢 контейнер есть
   [2] remnanode2    → 🟢 контейнер есть
   [3] remnanode3    → ⚪ контейнер отсутствует
   
   👉 Выберите ноду для удаления [1-3]: 
   ```

2. Покажет, что будет удалено:
   ```
   ⚠ Будет удалено:
   📁 Директория: /opt/remnanode
   🐳 Контейнер:  remnanode
   🌐 Сеть:       br-remnanode
   🔀 Routing:    правила для 172.20.0.0/16
   
   ❓ Подтвердите удаление (y/N): 
   ```

3. Выполнит безопасное удаление:
   - ✅ Остановка и удаление контейнера
   - ✅ Удаление UFW правил
   - ✅ Удаление systemd service
   - ✅ Очистка ip rules и routes
   - ✅ Удаление iptables правил (NAT/SNAT)
   - ✅ Удаление sysctl конфигурации
   - ✅ Удаление Docker network (если не используется)
   - ✅ Удаление директории

### Проверки безопасности

Скрипт удаления содержит множество проверок безопасности:

- 🛡️ Предотвращение удаления системных сетей (bridge, host, none)
- 🛡️ Предотвращение удаления системных интерфейсов (lo, docker0)
- 🛡️ Предотвращение удаления системных подсетей (127.0.0.0/8, 172.17.0.0/16)
- 🛡️ Проверка использования ресурсов другими контейнерами
- 🛡️ Валидация имен сетей (должны соответствовать паттерну)

---

## 🔒 Безопасность

### Запрет ICMP ping и базовая настройка UFW

```bash
bash <(curl -Ls https://raw.githubusercontent.com/ReeA11/remnawave-node-setup/refs/heads/master/security-setup.sh)
```

Этот скрипт:
- 🚫 Блокирует ICMP ping запросы
- 🔥 Настраивает базовые правила UFW
- 🛡️ Устанавливает политику по умолчанию (deny incoming)

### Рекомендации по безопасности

1. **Используйте уникальные SECRET_KEY**
   - Скрипт автоматически проверяет уникальность
   - Не используйте одинаковые ключи для разных нод

2. **Ограничьте доступ к портам**
   - UFW правила создаются точечно (по интерфейсу)
   - Порты доступны только на выбранном IP

3. **Обновление ноды**
   ```bash
   # Проверка версии ноды
   docker exec remnanode remnanode --version
   
   # Обновление до последней версии, если в контейнере указана версия latest
   cd /opt/remnanode
   docker compose pull
   docker compose up -d
   ```

4. **Мониторинг логов**
   ```bash
   # Просмотр логов
   docker logs -f remnanode
   
   # Или через docker-compose
   cd /opt/remnanode
   docker compose logs -f
   ```

---

## 🏗️ Архитектура

### Структура файлов

```
/opt/remnanode/
├── .env                    # Переменные окружения
├── docker-compose.yml      # Docker Compose конфигурация
└── data/                   # Данные контейнера (автоматически)

/etc/systemd/system/
└── remnanode-net-remnanode.service  # Systemd unit для network rules

/usr/local/sbin/
└── remnanode-net-remnanode.sh       # Скрипт восстановления routing правил

/etc/sysctl.d/
└── 99-remnanode-remnanode.conf      # Sysctl настройки (rp_filter)
```

### Компоненты системы

#### 1. Docker Compose
- Управление контейнером RemnaNode
- Проброс портов на выбранный IP
- Публикация Xray портов (443, 8443)

#### 2. Policy Routing
- Отдельная routing table для каждой ноды
- ip rules для source-based routing
- Корректная маршрутизация для multi-IP серверов

#### 3. NAT/SNAT
- MASQUERADE для docker subnet
- Правильный source IP для исходящего трафика
- ACCEPT rules в FORWARD chain

#### 4. Systemd Integration
- Автоматическое восстановление правил после перезагрузки
- Зависимость от docker.service
- Персистентность iptables правил

### Сетевая схема

```
┌────────────────────────────────────────────────────────┐
│ RemnaNode Server                                       │
│                                                        │
│  ┌───────────────┐         ┌───────────────┐           │
│  │   eth0        │         │   eth1        │           │
│  │ 10.0.0.5      │         │ 10.0.1.5      │           │
│  │ → 203.0.113.10│         │ → 203.0.113.20│           │
│  └──────┬────────┘         └──────┬────────┘           │
│         │                         │                    │
│         │  Policy Routing         │  Policy Routing    │
│         │  (Table 201)            │  (Table 202)       │
│         │                         │                    │
│  ┌──────▼────────┐         ┌──────▼────────┐           │
│  │  remnanode    │         │  remnanode2   │           │
│  │  172.20.0.2   │         │  172.21.0.2   │           │
│  │  Port: 2222   │         │  Port: 2222   │           │
│  └───────────────┘         └───────────────┘           │
│                                                        │
└────────────────────────────────────────────────────────┘
          ▲                         ▲
          │                         │
          │ Client connects to      │ Client connects to
          │ 203.0.113.10:2222       │ 203.0.113.20:2222
          │                         │
```

---

## 🔧 Устранение неполадок

### Проблема: Контейнер не запускается

**Проверка логов:**
```bash
docker logs remnanode
# или
cd /opt/remnanode && docker compose logs
```

**Типичные причины:**
1. Порт занят другим процессом
   ```bash
   ss -tulpn | grep :2222
   ```

2. Неверный SECRET_KEY
   ```bash
   cat /opt/remnanode/.env | grep SECRET_KEY
   ```

3. Проблемы с Docker
   ```bash
   systemctl status docker
   docker info
   ```

### Проблема: Нода показывается offline в панели

**Проверка сетевой связности:**
```bash
# Проверка доступности порта извне
curl -v http://YOUR_EXTERNAL_IP:2222

# Проверка routing правил
ip rule show
ip route show table 201

# Проверка NAT правил
iptables -t nat -L POSTROUTING -n -v
```

**Проверка источника трафика:**
```bash
# Из контейнера
docker exec remnanode curl ifconfig.me

# Должен показать правильный внешний IP
```

### Проблема: Конфликт портов при установке

**Решение:**
1. Выберите другой порт при установке
2. Или освободите порт:
   ```bash
   # Найти процесс
   ss -tulpn | grep :2222
   
   # Остановить процесс
   systemctl stop service_name
   # или
   docker stop container_name
   ```

### Проблема: UFW блокирует трафик

**Проверка правил UFW:**
```bash
ufw status numbered
```

**Добавление правила вручную:**
```bash
# Для API порта
ufw allow in on eth0 to 10.0.0.5 port 2222 proto tcp

# Для Xray портов
ufw allow in on eth0 to 10.0.0.5 port 443 proto tcp
ufw allow in on eth0 to 10.0.0.5 port 443 proto udp
ufw allow in on eth0 to 10.0.0.5 port 8443 proto tcp
ufw allow in on eth0 to 10.0.0.5 port 8443 proto udp
```

### Проблема: После перезагрузки сервера нода не работает

**Проверка systemd service:**
```bash
systemctl status remnanode-net-remnanode.service
```

**Ручной запуск скрипта восстановления:**
```bash
/usr/local/sbin/remnanode-net-remnanode.sh
```

**Перезапуск контейнера:**
```bash
cd /opt/remnanode
docker compose restart
```

---

## ❓ FAQ

### Можно ли установить несколько нод на одном сервере?
Да! Скрипт автоматически создаёт директории `/opt/remnanode`, `/opt/remnanode2`, `/opt/remnanode3` и т.д. Каждая нода получает:
- Уникальную директорию
- Уникальное имя контейнера
- Уникальные routing правила

### Какие порты использует RemnaNode?

- **Основной порт**: Порт API (по умолчанию 2222, настраивается)
- **Xray порты**: Стандартно 443 и 8443 (tcp/udp) для клиентских подключений

### Как обновить ноду до новой версии?
```bash
cd /opt/remnanode
docker compose down
# Измените NODE_VERSION в .env или docker-compose.yml
docker compose pull
docker compose up -d
```

### Можно ли использовать на сервере с одним IP?
Да, скрипт работает как на серверах с одним IP, так и с несколькими. Policy routing и NAT настраиваются автоматически в обоих случаях.

### Что делать если установка прервалась?
Скрипт использует staging директорию и cleanup функции. При ошибке:
1. Staging директория будет автоматически удалена
2. Частично созданные ресурсы будут очищены
3. Можно безопасно запустить установку заново

### Как посмотреть какие ноды установлены?
```bash
# Список директорий
ls -la /opt/ | grep remnanode

# Список контейнеров
docker ps -a | grep remnanode

# Через меню удаления
bash <(curl -Ls https://raw.githubusercontent.com/ReeA11/remnawave-node-setup/refs/heads/master/remove-remnanode.sh)
```

### Поддерживается ли IPv6?
В текущей версии скрипт работает только с IPv4. Поддержка IPv6 планируется в будущих обновлениях.

### Можно ли изменить порты после установки?
Да, для этого необходимо:
1. Отредактировать `.env` и `docker-compose.yml`
2. Перезапустить контейнер: `docker compose up -d`
3. Обновить UFW правила (если используется)

### Скрипт удаляет Docker при удалении ноды?
Нет, скрипт удаляет только:
- Контейнер RemnaNode
- Docker network ноды (если не используется другими контейнерами)
- Routing и NAT правила
- UFW правила ноды
- Директорию ноды

Docker и другие контейнеры остаются нетронутыми.

---

## 📜 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

---

<div align="center">

**Сделано с ❤️ для Remnawave сообщества**

[⬆ Наверх](#-remnanode-setup)

</div>